<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splashscreen"
    android:layout_width="match_parent"
    android:background="#FFFFFF"
    android:layout_height="match_parent"
    tools:context="com.example.asus.iscan.splashscreen"
    >


    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/logo1"
        android:id="@+id/imageView6"
        tools:ignore="ContentDescription"
        android:background="#FFFFFF"
        android:layout_centerVertical="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:text="LOADING. . ."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:id="@+id/textView2"
        tools:ignore="HardcodedText"
        android:textColor="#000000"/>

    <ProgressBar
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_height="40dp"
        android:id="@+id/load"
        android:backgroundTint="@color/colorPrimary"
        tools:backgroundTint="@color/colorPrimary"
        tools:ignore="UnusedAttribute"
        tools:style="@style/Widget.AppCompat.ProgressBar"
        android:indeterminateOnly="?attr/isLightTheme"
        android:maxHeight="@dimen/nav_header_vertical_spacing"
        android:progressBackgroundTint="@color/colorAccent"
        android:progressDrawable="@color/colorAccent"
        android:progressTint="@color/colorAccent"
        android:layout_width="800dp"
        android:elevation="15dp"


        android:indeterminateDrawable="@android:drawable/progress_indeterminate_horizontal"
        android:visibility="visible"
        tools:visibility="visible"
        android:max="?android:attr/scrollbarFadeDuration"
        android:indeterminate="true"
        android:layout_marginBottom="19dp"
        android:layout_above="@+id/textView2"
        android:layout_alignParentStart="true" />


</RelativeLayout>
